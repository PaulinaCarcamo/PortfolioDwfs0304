import * as React from 'react';
import WebCards from '../components/WebCards.js';

const Projects = () => {
    return (
        <React.Fragment>
            <WebCards />
        </React.Fragment>
    );
}

export default Projects;
