import * as React from 'react';
import WebCards from '../components/WebCards.js';

const Projects = () => {
    return (
        <React.Fragment>
            <div id="projects">
                <WebCards />
            </div>
        </React.Fragment>
    );
}

export default Projects;
